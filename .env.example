# Gemini Business API 配置示例
# 复制此文件为 .env 并填入实际值

# 必须配置的环境变量
BEARER_TOKEN=your_actual_bearer_token_here
CONFIG_ID=your_config_id_here

# 可选配置的环境变量
PROXY_URL=
DEBUG=false
PORT=8080

# 使用说明：
# 1. 从浏览器开发者工具中获取 Bearer Token
# 2. 从Gemini Business页面URL中获取 ConfigID
# 3. 运行: docker-compose up -d
# 4. 查看日志: docker logs -f gemini-proxy
# 5. 测试: curl http://localhost:8080/v1/chat/completions -H "Content-Type: application/json" -d '{"model":"gemini-2.5-flash","messages":[{"role":"user","content":"你好"}],"stream":true}'